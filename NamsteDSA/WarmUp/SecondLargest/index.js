
//? write a function that returns the second largest number from an

let arr = [4, 9, 0, 2, 8, 7, 1];

function secondLargest(arr) {

    // let largest = arr[0]
    // for (let i = 0; i < arr.length; i++) {
    //     if (arr[i] > largest) {
    //         largest = arr[i]
    //     }
    // }
    // let bigNumber =  largest;

    // let secondLarge = arr[0] - bigNumber;

    // for (let i = 0; i < arr.length; i++){
    //     if (arr[i] < secondLarge) {
    //         let secondLarge = arr[i]
    //     }
    // }
    // return secondLarge
}

let result = secondLargest(arr);
console.log(result);

